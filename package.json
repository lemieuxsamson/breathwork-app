{
  "name": "breathwork-app",
  "version": "1.5.1",
  "private": true,
  "description": "Application web de respiration guidée (cohérence cardiaque & breathwork), PWA hors ligne, sans dépendance externe.",
  "scripts": {
    "test": "node --test",
    "check-versions": "node scripts/check-versions.js",
    "sync-version": "node scripts/sync-version.js",
    "verify": "npm run check-versions && npm test",
    "version": "npm run sync-version"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "jsdom": "^29.0.0"
  },
  "license": "MIT"
}
